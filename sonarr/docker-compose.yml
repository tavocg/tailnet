services:
  ts-sonarr:
    image: tailscale/tailscale:latest
    container_name: ts-sonarr
    restart: unless-stopped
    hostname: ${TS_HOSTNAME}
    environment:
      - TS_AUTHKEY
      - TS_EXTRA_ARGS
      - TS_SERVE_CONFIG=/ts/serve.json
    volumes:
      - tailscale:/var/lib/tailscale
    ports:
      - 8989:8989
    devices:
      - /dev/net/tun:/dev/net/tun
    cap_add:
      - net_admin
      - sys_module
    configs:
      - source: ts-config
        target: /ts/serve.json
    networks:
      - arr_network

  sonarr:
    image: lscr.io/linuxserver/sonarr:latest
    container_name: sonarr
    restart: unless-stopped
    network_mode: service:ts-sonarr
    depends_on:
      - ts-sonarr
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-America/Chicago}
    volumes:
      - ${SONARR_CONFIG_DIR:-config}:/config
      - ${SONARR_TV_DIR:-/mnt/storage/media/shows}:/tv
      - ${SONARR_DOWNLOADS_DIR:-/mnt/storage/data/qbittorrent/downloads}:/downloads

volumes:
  tailscale:
  config:

networks:
  arr_network:
    name: arr_network
    external: true

configs:
  ts-config:
    content: |
      {
        "TCP": {
          "443": {
            "HTTPS": true
          }
        },
        "Web": {
          "$${TS_CERT_DOMAIN}:443": {
            "Handlers": {
              "/": {
                "Proxy": "http://127.0.0.1:8989"
              }
            }
          }
        },
        "AllowFunnel": {
          "$${TS_CERT_DOMAIN}:443": ${TS_ALLOW_FUNNEL:-false}
        }
      }
