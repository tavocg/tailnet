services:
  ts-lidarr:
    image: tailscale/tailscale:latest
    container_name: ts-lidarr
    restart: unless-stopped
    hostname: ${TS_HOSTNAME}
    environment:
      - TS_AUTHKEY
      - TS_EXTRA_ARGS
      - TS_SERVE_CONFIG=/ts/serve.json
    volumes:
      - tailscale:/var/lib/tailscale
    ports:
      - 8686:8686
    devices:
      - /dev/net/tun:/dev/net/tun
    cap_add:
      - net_admin
      - sys_module
    configs:
      - source: ts-config
        target: /ts/serve.json
    networks:
      - arr_network

  lidarr:
    image: lscr.io/linuxserver/lidarr:latest
    container_name: lidarr
    restart: unless-stopped
    network_mode: service:ts-lidarr
    depends_on:
      - ts-lidarr
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-America/Chicago}
    volumes:
      - ${LIDARR_CONFIG_DIR:-config}:/config
      - ${LIDARR_MUSIC_DIR:-/mnt/storage/media/music}:/music
      - ${LIDARR_DOWNLOADS_DIR:-/mnt/storage/data/qbittorrent/downloads}:/downloads

volumes:
  tailscale:
  config:

networks:
  arr_network:
    name: arr_network
    external: true

configs:
  ts-config:
    content: |
      {
        "TCP": {
          "443": {
            "HTTPS": true
          }
        },
        "Web": {
          "$${TS_CERT_DOMAIN}:443": {
            "Handlers": {
              "/": {
                "Proxy": "http://127.0.0.1:8686"
              }
            }
          }
        },
        "AllowFunnel": {
          "$${TS_CERT_DOMAIN}:443": ${TS_ALLOW_FUNNEL:-false}
        }
      }
